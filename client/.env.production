# Production Environment Variables for Client

# API Base URL (optional - auto-detected if not set)
# In production, defaults to relative URL "/api" (same domain)
# REACT_APP_API_URL=/api

# OR if backend on different domain:
# REACT_APP_API_URL=https://api.yourdomain.com/api
